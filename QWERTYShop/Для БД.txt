create table public.cards
(
    Id bigint,
    Name text,
    Type text,
    AddedTime timestamp,
    Image text,
    Information text,
    Cost int
);

create table public.citylist(	City varchar, IsAvailableForPickup bool, CostForDelivery int);
